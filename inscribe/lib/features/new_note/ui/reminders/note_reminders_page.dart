import 'package:flutter/material.dart';

class NoteRemindersPage extends StatelessWidget {
  const NoteRemindersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(20),
      child: Column(
        children: [
          Text("TODO"),
        ],
      ),
    );
  }
}
